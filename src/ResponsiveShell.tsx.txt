// src/components/ResponsiveShell.tsx
export default function ResponsiveShell({ left, center, right, footer }: any) {
  return (
    <div className="min-h-screen grid grid-rows-[1fr_auto]">
      <main className="grid gap-3 p-3 
                       grid-cols-1 
                       md:grid-cols-2 
                       xl:grid-cols-3">
        <section className="order-1">{left}</section>
        <section className="order-2">{center}</section>
        <section className="order-3 xl:block hidden">{right}</section>
      </main>
      <nav className="sticky bottom-0 w-full border-t bg-white/90 backdrop-blur p-2
                      grid grid-cols-3 gap-2">
        {footer}
      </nav>
    </div>
  );
}
